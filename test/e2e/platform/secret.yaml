apiVersion: v1
kind: Secret
metadata:
  name: github-app
  namespace: platform-service-gitops-system
data:
  appID: "{{ .AppIDBase64 }}"
  url: "{{ .URLBase64 }}"
  privateKey: "{{ .PrivateKeyBase64 }}"
